
#I want data or views to handle caching
#and cache invalidation
#views handles

#and correctly instantiation the type of
#object that is specified in the type

CureViews ->
  @render = ( args ) ->

    #look in the cache for this object 
    #if availble then use that
    #otherwise pull it out of the db

    #call the render function on the view
    #given the view name and context

    args.collection
    args.name
    args.view
    args.context
