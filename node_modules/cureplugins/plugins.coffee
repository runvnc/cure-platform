console.log 'Current working directory is ' + process.cwd()
console.log 'Reading file plugins.json'

fs = require 'fs'
util = require 'util'
_u = require 'underscore'

toLoadStr = fs.readFileSync __dirname + '/plugins.json'
toLoad = JSON.parse toLoadStr
console.log 'Loading plugins: ' + util.inspect toLoad

loadAnyTypes = (plugin) ->
  dummy = 1

load = (plugin) ->
  try
    require plugin + '/server'
  catch e
    console.log 'Error loading plugin "' + plugin + '" ' + e.message

load plugin for plugin in toLoad

